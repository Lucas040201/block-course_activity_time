{{#users}}
    <div class="row pt-2 students-item table-row">
        <div class="col-3 text-center pt-2 pb-2">
            <a href="{{userUrl}}" target="_blank">{{fullname}}</a>
        </div>
        <div class="col-3 text-center pt-2 pb-2">
            <a href="{{userUrl}}" target="_blank">{{email}}</a>
        </div>
        <div class="col-3 text-center">
            {{{totaltime}}}
        </div>
        <div class="col-3 text-center">
            {{progress}}
        </div>
    </div>
{{/users}}